using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScriptDeReferencia : MonoBehaviour {

    // consejos
    // - que el mapa de juego este situado dentro del epacio positivo de ambos ejes (que su esquina inferior izquierda este en el punto 0,0)
    // - cada uno de los cuadrados de la grid cuadre con los de la grid de referencia de unity (para esto miramos el ancho (px) del grid de referencia y lo dividimos entre la cantidad de cuadrados que tiene. 251 / 28 = x y este x lo ponemos en su variable Pixel Per Unit
    // - 


	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
